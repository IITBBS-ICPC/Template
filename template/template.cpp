#include "icpch/base.hpp"  // Replace this line with contents of base.hpp before
                           // submitting
#ifdef MYCODER_JUDGE
#include "icpch/debug.hpp"
#else
#define debug(...) 69
#endif  // MYCODER_JUDGE

void solve() {}

int main() {
    int ti, tc;
    FAST_IO;
    cin >> tc;
    for (ti = 1; ti <= tc; ti++) solve();
}
